#include <iostream>
#include<string>
using namespace std;
int N;
char start = 'a';
string str;

int main() {
	ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
	cin >> N;
	for (int i = 0; i < N; i++) {
		cout << 'a';
	}
}
